# Copyright 2015 6WIND S.A.

# use the branch under review for vif_plug_script
NOVA_REPO=https://review.openstack.org/p/openstack/nova
NOVA_BRANCH=refs/changes/70/162470/9

# fast path settings
FP_PKG_DIR=${FP_PKG_DIR:-$DEST/fp-delivery}
FP_MASK=${FP_MASK:-0x4}
FP_MEMORY=${FP_MEMORY:-1024}
FP_NB_MEM_CHANNELS=${FP_NB_MEM_CHANNELS:-4}
FP_NB_HUGEPAGES=${FP_NB_HUGEPAGES:-2048}
FP_NB_MBUF=${FP_NB_MBUF:-65536}
FP_NB_VHOSTUSER_PORTS=${FP_NB_VHOSTUSER_PORT:-20}

# openvswitch settings
OVS_PHYSICAL_INTERFACE=${OVS_PHYSICAL_INTERFACE:-''}

# set ovs-fp mechanism driver
Q_ML2_PLUGIN_MECHANISM_DRIVERS=ovs-fp

# disable security group and ovs hybrid for better networking performance
Q_DISABLE_SECURITY=${Q_DISABLE_SECURITY:-False}
