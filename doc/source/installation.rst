============
Installation
============

At the command line::

    $ pip install networking-6wind

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv networking-6wind
    $ pip install networking-6wind
